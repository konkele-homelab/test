name: Swarm Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - swarm
      - dev
    steps:
      - name: Sanity check
        run: |
          echo "Runner name: $RUNNER_NAME"
          echo "Labels: $RUNNER_LABELS"
          hostname
          uname -a
